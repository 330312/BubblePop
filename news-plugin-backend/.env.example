# Server
PORT=8787

# CORS
# For extension dev, you can set this to "*".
# In production, set to your allowed origins, separated by comma.
CORS_ORIGINS=*

# DuckDuckGo (via python/ddg_search.py)
DDG_REGION=cn-zh
DDG_BACKEND=auto
DDG_TIMEOUT_MS=12000

# GDELT
GDELT_TIMEOUT_MS=8000
# Set to 1 if local TLS inspection breaks GDELT requests
GDELT_INSECURE=0

# Python path for DDG/Agent runner
SEARCH_PYTHON=

# =========================
# Agent (member D) optional
# =========================
# Mode options:
# - http:   call AGENT_URL (agent is a web service)
# - process: spawn python runner (python/agent_runner.py) which imports python/Agent.py
# - none:   disable agent and use fallback analysis
AGENT_MODE=process

# --- http mode ---
AGENT_URL=

# --- process mode ---
# On Windows you typically use: python
# On macOS/Linux you typically use: python3
# AGENT_PYTHON=D:\Anaconda\envs\bubblepop\python.exe
# You can change this if you relocate the runner
AGENT_RUNNER=python/agent_runner.py

# Agent timeout
AGENT_TIMEOUT_MS=120000

# Debug (print agent raw output)
AGENT_DEBUG=0

CONTENTS_MAX_CHARS=8000
# ARTICLE_MAX_CHARS=6500

# 默认还是 5000 也行，不写也行
AGENT_MAX_CHARS_DEFAULT=6000

# stances（B步）给少点，避免它吃掉太多正文前段
AGENT_MAX_CHARS_STANCES=4000

# relatedEvents（C步）给多点，让它看到更多背景/正文
AGENT_MAX_CHARS_RELATED_EVENTS=6000

# Member D's Agent.py needs a Zhipu/ZAI key. Set ONE of these.
ZAI_API_KEY=
